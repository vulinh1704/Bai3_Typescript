import Manager from "./manager";
import {Student} from "./student";

class ManageStudent implements Manager<Student>{
    add(t: Student) {
    }

    findAll() {
    }

    findById(id: number) {
    }

}